import React from "react";

const Myprops = (props) => {
    return (
        <div>
            <li>Example 1: My name is <strong>{props.name}</strong></li>
        </div>

    );
};

export default Myprops;